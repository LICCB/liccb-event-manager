{% extends 'base.html.twig' %}

{% block body %}

	<h1>Permission Matrix</h1>
	{{ form_start(form) }}

	{#{% for permission in form.permissions %}
		{{ form_row(permission.rolesFromArray) }}
	{% endfor %} #}
	{{ form_widget(form) }}
	{{ form_end(form) }}

{% endblock %}